{
  "name": "edupath-workspace",
  "version": "1.0.0",
  "description": "EduPath - Adaptive, AI-driven skill gap and personalized learning path agent",
  "scripts": {
    "postinstall": "npm --prefix server install --include=dev && npm --prefix frontend install --include=dev",
    "dev:server": "npm --prefix server run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:frontend\"",
    "build:server": "npm --prefix server run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:server && npm run build:frontend",
    "start": "node server/dist/index.js",
    "test:api": "npm --prefix server run test:api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
